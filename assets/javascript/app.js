$(document).ready(function() {

    //global variables
    var zipCode = 0;
    var difficultyLevel = "";
    var queryURLTrail = "";
    var queryURLWeather = "";

    //create on click function for the submit button
    //create a zipcode validation function
    //create a function to convert zipcode to long lat
    //
}